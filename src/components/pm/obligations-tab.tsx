'use client'

/**
 * «Υποχρεώσεις & Δικαιολογητικά» tab (Task 10 — stub). Πραγματικό σώμα
 * (λίστα υποχρεώσεων ανά στάδιο, CRUD, ανέβασμα εγγράφων) προστίθεται στο
 * Task 12.
 */
export function ObligationsTab({ applicationId, canManage }: { applicationId: string; canManage: boolean }) {
  void applicationId
  void canManage
  return (
    <section className="glass rounded-[22px] p-4">
      <div className="p-6 text-center text-sm text-muted-foreground">(σε εξέλιξη)</div>
    </section>
  )
}
