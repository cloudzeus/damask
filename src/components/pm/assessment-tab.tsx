'use client'

/**
 * «Αξιολόγηση» tab (Task 10 — stub). Πραγματικό σώμα (λίστα κριτηρίων με
 * βαθμολόγηση, recomputeAssessment, verdict) προστίθεται στο Task 11.
 */
export function AssessmentTab({ applicationId, canManage }: { applicationId: string; canManage: boolean }) {
  void applicationId
  void canManage
  return (
    <section className="glass rounded-[22px] p-4">
      <div className="p-6 text-center text-sm text-muted-foreground">(σε εξέλιξη)</div>
    </section>
  )
}
