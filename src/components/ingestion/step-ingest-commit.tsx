'use client'

import type { StepProps } from './types'

export function StepIngestCommit(_props: StepProps & { onDone?: () => void }) {
  return <div className="p-6 text-center text-sm text-muted-foreground">(σε εξέλιξη)</div>
}
