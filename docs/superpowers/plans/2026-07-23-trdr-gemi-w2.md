# W2 — ΓΕΜΗ/ΑΑΔΕ/Geo στους Συναλλασσόμενους — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Spec:** `docs/superpowers/specs/2026-07-23-trdr-gemi-w2-design.md` (READ FULLY — it is the contract). **Ref repo:** `/private/tmp/claude-501/-Volumes-EXTERNALSSD-DGSMART-damask/5f092e05-2323-408e-849a-70d57b13a320/scratchpad/pb-ref` — `lib/gemi.ts`, `app/api/admin/{aade-lookup,gemi-lookup}/route.ts`, `app/api/admin/companies/[id]/gemi-sync/route.ts`, `app/api/admin/metadata/refresh-gemi/route.ts`, `app/admin/companies/*`.

**DAMASK ground rules:** tests in `tests/`; Prisma 7.8 hand-edit (no `prisma format`), revert unrelated reformatting, `yes | npx prisma migrate dev`; every action gated (REUSE the partners permissions — read `src/lib/objects.ts` partners item); pure files no prisma/react/clock; `bunnyUploadPrivate({key,body,contentType})`/`bunnyDownload`; settings via `getIntegration`/`setIntegration` (read `src/lib/settings.ts` + how the mailgun/maps cards work in `src/app/(app)/settings`); W1 building blocks: `resolveKadForActivity`/`ensurePrimaryActivity` (`@/lib/registries/kad*`), `matchRegion` (`@/lib/registries/regions.ts`); Trdr surface `/partners` (`src/app/(app)/partners/*`, `appLat/appLng` exist); don't stage `.planning/HANDOFF.json`/`vitest.config.ts`; ambient RouteContext tsc error — ignore.

## T1 Schema
Spec §0.1-0.4: Trdr extends (app-extras block, reuse appLat/appLng — DO NOT add lat/lng), `ActivityKind`/`DocumentSource`/`TrdrDocumentKind` enums (multi-line), `TrdrKad`, `TrdrDocument`, `LegalType`/`GemiOfficeRef`/`CompanyStatusRef`/`PrefectureRef`/`MunicipalityRef`, `Region.trdrs` back-relation. TDD `tests/trdr-gemi-schema.test.ts` (dmmf + runtime enums). Migration `trdr_gemi_w2`. Commit.

## T2 Clients + settings
`src/lib/trdr/gemi.ts` (port ref lib/gemi.ts: GemiError, metadata.*, searchCompanies, getCompany, getCompanyDocuments, downloadGemiFile, mapGemiCompany→**Trdr shape** — adapt field names to our Trdr/spec §0.1; api_key from `getIntegration<{apiKey?:string}>('gemi')`, isGemiConfigured()) + `src/lib/trdr/aade.ts` (port aade-lookup route body: afm2info POST, s() nil helper, mapped+activities). Add `'gemi'` to `IntegrationName` + defaults case in settings.ts. Settings UI: ΓΕΜΗ card in Διασυνδέσεις (apiKey field + «Δοκιμή σύνδεσης» action calling `metadata.legalTypes()` — mirror the existing card+test-connection idiom exactly). TDD pure parts (`tests/trdr-gemi-map.test.ts`: mapGemiCompany fixture→Trdr shape, aade s() coercion + activity kind mapping). Commit.

## T3 Enrich actions
`src/lib/trdr/enrich-actions.ts` ('use server') per spec §0.7 — ALL gated with the partners permission (find exact string in objects.ts; the partners actions file `src/app/(app)/partners/actions.ts` shows the idiom — mirror it). gemiSyncTrdr: transaction update Trdr + delete/createMany TrdrKad (resolve via W1, dedupe PRIMARY-preferred) + per-doc download→bunny→upsert (non-fatal per doc, counts). applyAadeToTrdr similar (no docs). matchTrdrRegion: build MatchInput from Trdr (+gemiData descrs if present) → write regionCode+confidence in notes? (just regionCode). bulkMatchTrdrRegions: iterate regionCode-null, tally confidence, non-fatal per row. listTrdrGemiDocuments (live preview), saveTrdrGemiDocument (one doc→bunny→TrdrDocument), listTrdrDocuments, removeTrdrDocument. Download route `src/app/(app)/partners/[id]/documents/[docId]/route.ts` (mirror pm documents route gating with partners perm). TDD: guards (`tests/trdr-enrich-guard.test.ts` all actions reject) + logic (`tests/trdr-gemi-sync.test.ts` hoisted mocks: sync updates+replaces kads with PRIMARY dedupe+upserts docs; match writes regionCode; bulk tallies; save single doc). Commit.

## T4 UI
(α) `/partners` table (`partners-table.tsx`/`partner-row-actions.tsx`): row actions «ΑΑΔΕ έλεγχος» (dialog preview→apply), «Συγχρονισμός ΓΕΜΗ» (confirm→run→toast counts), «Εντοπισμός Περιφέρειας» (run→toast breadcrumb)· στήλη «Περιφέρεια» (badge nameEL αν regionCode — include στο list query)· toolbar «Μαζικός εντοπισμός περιφερειών» (confirm→bulk→toast tallies). (β) καρτέλα `/partners/[id]/page.tsx`: νέα sections/cards — «ΓΕΜΗ & ΑΑΔΕ» (πεδία read-only + sync κουμπιά + timestamps), «ΚΑΔ» (TrdrKad table: κωδικός/περιγραφή/PRIMARY badge/«Άδεια λειτουργίας» flag — join KadLicenseRequirement), «Έγγραφα» (saved TrdrDocument list με download route + «Προβολή εγγράφων ΓΕΜΗ» on-demand dialog με «Αποθήκευση» per item). (γ) settings card από T2 αν δεν έγινε εκεί. Follow the existing καρτέλα structure/design (READ `src/app/(app)/partners/[id]/page.tsx` first). tsc/build/tests. Commit.

## T5 Review + merge prep
Full suite/tsc/build. Holistic review: ΓΕΜΗ key ΜΟΝΟ από settings (no env/hardcode — grep), gemiSync id-safety (trdrId scoped, docs keyed record-side), TrdrKad PRIMARY dedupe correctness vs ref, matchRegion wiring fidelity, download route gated, no new permissions, migration additive. Fix CRITICAL/IMPORTANT → finishing-a-development-branch. **Post-merge (controller):** seed `integration.gemi` apiKey (one-off, εκτός repo)· ΔΕΝ χρειάζεται sync-permissions.
